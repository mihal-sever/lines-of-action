using UnityEngine;

public class CheckerSpawner : MonoBehaviour
{
    //public void SpawnCheckers(Player player, Board board)
    //{
    //    foreach (Vector2Int v in player.OpeningPositions)
    //    {
    //        CreateChecker(player, board.Cells[v.x, v.y]);
    //    }
    //}

    //private void CreateChecker(Player player, Cell cell)
    //{
    //    GameObject instance = Instantiate(player.checkerPrefab);
    //    Checker checker = instance.GetComponent<Checker>();
    //    checker.Move(cell);
    //    player.AddChecker(checker);
    //    //checkers.Add(checker);
    //    //checker.name = "checker " + checkers.Count;
    //}
}
